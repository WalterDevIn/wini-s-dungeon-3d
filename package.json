{
  "name": "wini-s-dungeon-3d",
  "version": "0.0.0-vpreliminar",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace client\" \"npm run dev --workspace server\""
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
